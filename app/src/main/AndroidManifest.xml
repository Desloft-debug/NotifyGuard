<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Нужно, чтобы отличать контакты от незнакомых номеров -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Нужно, чтобы показать список установленных приложений в белом списке -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_lock_silent_mode"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotifyGuard">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Читает и снимает уведомления. Доступ выдаётся вручную в настройках системы. -->
        <service
            android:name=".GuardNotificationListener"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Опционально: гасит уведомление ДО показа, чтобы не мигал баннер.
             Работает, только если приложение выбрано ассистентом уведомлений. -->
        <service
            android:name=".GuardAssistantService"
            android:exported="true"
            android:label="@string/assistant_label"
            android:permission="android.permission.BIND_NOTIFICATION_ASSISTANT_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationAssistantService" />
            </intent-filter>
        </service>

        <!-- Приглушает звонки не из контактов -->
        <service
            android:name=".GuardCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>
    </application>
</manifest>
